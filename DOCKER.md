# دليل تشغيل النظام بضغطة واحدة باستخدام Docker (Docker One-Click Run)

لقد قمنا بتجهيز ملفات **Dockerfile** و **docker-compose.yml** لتمكينك من تشغيل النظام بالكامل (تطبيق Next.js + قاعدة بيانات MySQL) بضغطة واحدة وبشكل تلقائي دون أي إعداد يدوي.

---

## المتطلبات الأساسية
* تأكد من تثبيت **Docker** و **Docker Compose** على خادمك أو جهازك.

---

## كيف يعمل التشغيل التلقائي؟
عند تشغيل Docker Compose، سيقوم النظام بالآتي تلقائياً:
1. بناء وتجهيز خادم قاعدة بيانات MySQL.
2. تشغيل التطبيق والانتظار حتى تصبح قاعدة البيانات جاهزة لاستقبال الاتصالات.
3. تشغيل سكربت تهيئة الجداول (`init_db.js`) لبناء الجداول تلقائياً إن لم تكن موجودة.
4. تغذية الجداول بالبيانات الأولية والتجريبية (`seed_mysql.mjs`).
5. تشغيل النظام على المنفذ `80` مباشرة.

---

## الإعدادات الهامة قبل التشغيل (Configuration)
قبل البدء في تشغيل النظام، يجب عليك تعديل بعض المتغيرات البيئية داخل ملف [docker-compose.yml](file:///d:/Macro%20Workflow System/workflow-engine/docker-compose.yml) ليناسب خادمك:

* **`NEXTAUTH_URL`**: افتراضياً يكون `http://localhost`. **يجب تغييره** إلى عنوان الـ IP الحقيقي الخاص بالسيرفر (مثل `http://192.168.3.48`) أو اسم النطاق الخاص بك (Domain) لتجنب حدوث أخطاء تحويل عند تسجيل الدخول.
* **`NEXTAUTH_SECRET`**: عبارة عن مفتاح تشفير عشوائي يفضل تغييره لحماية الجلسات وتأمين تسجيل الدخول.

---

## طريقة التشغيل (خطوة واحدة):

انتقل إلى مجلد المشروع `workflow-engine` في السيرفر ثم اكتب الأمر التالي:

```bash
docker compose up -d --build
```

### شرح الأمر:
* `up`: تشغيل جميع الحاويات (Containers).
* `-d`: تشغيل في الخلفية (Background) حتى لا يتوقف عند إغلاق التيرمنال.
* `--build`: إعادة بناء كود التطبيق لضمان تشغيل أحدث تعديلات قمت بها.

---

## روابط الوصول للنظام:
* **رابط الموقع**: افتح المتصفح واكتب `http://localhost` أو الـ IP الخاص بالسيرفر مباشرة (بدون كتابة أي رقم منفذ لأن التطبيق يعمل على المنفذ 80 الافتراضي).
* **منفذ قاعدة البيانات**: المنفذ `3306` مفتوح للاتصال بقاعدة البيانات محلياً أو عن بُعد ببيانات الاتصال التالية:
  - **اسم المستخدم**: `emacro_user`
  - **كلمة المرور**: `emacro_password_strong`
  - **قاعدة البيانات**: `emacro_dashboard`

---

## أوامر مفيدة للتحكم:

* **إيقاف تشغيل النظام بالكامل**:
  ```bash
  docker compose down
  ```

* **عرض ومتابعة سجل التشغيل (Logs) لمتابعة حالة السيرفر**:
  ```bash
  docker compose logs -f app
  ```

* **إعادة تشغيل النظام**:
  ```bash
  docker compose restart
  ```
